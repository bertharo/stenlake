# Database
# For Vercel/Production: Use Postgres connection string (from Vercel Postgres or other provider)
# For Local Dev: Can use SQLite: "file:./prisma/dev.db" (change schema.prisma provider to "sqlite")
DATABASE_URL="postgresql://user:password@localhost:5432/stenlake"

# Strava OAuth (optional)
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI="http://localhost:3000/api/auth/strava/callback"

# OpenAI API (optional)
OPENAI_API_KEY=
